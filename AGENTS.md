# Tetra AI Agent Instructions

## Mandatory Project Context

Before making any code changes, you MUST read:

- `TETRA_PROJECT_SKILL.md`

This file is the authoritative project-specific knowledge
for architecture, conventions, API contracts, authentication,
database models, Socket.IO behavior, and known implementation
caveats.

Do NOT ask the user to attach this file manually.

Do NOT assume the architecture from general knowledge.

For every task:

1. Read `TETRA_PROJECT_SKILL.md`.
2. Inspect the relevant source files.
3. Trace the existing data flow.
4. Implement the smallest compatible change.
5. Preserve existing architecture unless explicitly asked
   to redesign it.
6. Run appropriate validation.
7. Report what changed and what was verified.
